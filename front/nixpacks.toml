[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm install -g npm@latest", "npm install -g serve@14.2.3", "npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "ls -la dist && serve -s dist -l 0.0.0.0:$PORT"

