# PSBrand - نظام إدارة طلبات تصميم البلايز

نظام شامل لإدارة طلبات تصميم البلايز مع واجهة عربية كاملة ودعم RTL.

## 🚀 المميزات

### للموظفين
- 🔐 **تسجيل دخول آمن** (الأدمن يحدد Username و Password)
- إنشاء طلبات جديدة مع تفاصيل كاملة
- إدخال معلومات الزبون (الاسم، الرقم، الموقع، تفاصيل إضافية)
- تحديد تفاصيل الطلب (المقاس، اللون، السعر)
- رفع صور التصميم
- عرض الطلبات السابقة
- إحصائيات شخصية

### للأدمن
- 🔐 **تسجيل دخول آمن** (username: admin | password: admin)
- عرض جميع الطلبات
- معرفة الموظف المرسل وتاريخ ووقت الطلب
- تفاصيل الطلب كاملة مع الصور
- تحديث حالة الطلبات (قيد الانتظار / مكتمل / ملغي)
- إدارة حسابات الموظفين (إضافة / حذف)
- إحصائيات شاملة

## 🛠️ التقنيات المستخدمة

- **React 18** - مكتبة واجهة المستخدم
- **Vite** - أداة البناء السريعة
- **Material-UI (MUI)** - مكونات الواجهة
- **React Router** - التنقل بين الصفحات
- **React Hook Form** - إدارة النماذج
- **Zustand/Context API** - إدارة الحالة
- **Axios** - الاتصال مع الباك اند
- **Emotion** - CSS-in-JS مع دعم RTL

## 📁 هيكل المشروع

```
PSBrand/
├── src/
│   ├── components/
│   │   ├── common/          # مكونات مشتركة
│   │   ├── employee/        # مكونات الموظف
│   │   │   └── OrderForm.jsx
│   │   └── admin/           # مكونات الأدمن
│   │       ├── OrdersList.jsx
│   │       └── EmployeeManagement.jsx
│   ├── pages/
│   │   ├── RoleSelection.jsx
│   │   ├── EmployeeDashboard.jsx
│   │   └── AdminDashboard.jsx
│   ├── context/
│   │   └── AppContext.jsx   # إدارة الحالة العامة
│   ├── services/
│   │   └── api.js           # خدمات API
│   ├── theme/
│   │   └── theme.js         # ثيم Material-UI
│   ├── App.jsx
│   ├── main.jsx
│   └── index.css
├── package.json
└── README.md
```

## 🚀 البدء

### المتطلبات
- Node.js (الإصدار 18 أو أحدث)
- npm أو yarn

### التثبيت

1. استنساخ المشروع:
```bash
git clone <repository-url>
cd PSBrand
```

2. تثبيت المكتبات:
```bash
npm install
```

3. تشغيل المشروع:
```bash
npm run dev
```

4. فتح المتصفح على:
```
http://localhost:5173
```

## 📦 البناء للإنتاج

```bash
npm run build
```

سيتم إنشاء ملفات الإنتاج في مجلد `dist/`.

## 🔌 الربط مع الباك اند

المشروع جاهز للربط مع الباك اند .NET. الخدمات موجودة في:
- `src/services/api.js`

قم بتحديث `API_BASE_URL` في الملف حسب عنوان API الخاص بك.

### مثال على API Endpoints المطلوبة:

```
POST   /api/auth/login
POST   /api/auth/logout
GET    /api/orders
POST   /api/orders
PATCH  /api/orders/:id/status
GET    /api/employees
POST   /api/employees
DELETE /api/employees/:id
```

## 🎨 التخصيص

### تغيير الألوان
عدّل ملف `src/theme/theme.js` لتغيير الألوان والثيم.

### تغيير الخطوط
عدّل ملف `index.html` و `src/index.css` لاستيراد خطوط مختلفة.

## 📝 الملاحظات

- المشروع يستخدم **RTL (Right-to-Left)** بشكل كامل
- جميع النصوص باللغة العربية
- التصميم responsive لجميع الشاشات
- البيانات حالياً مخزنة في Context (مؤقتاً)
- جاهز للربط مع الباك اند .NET

## 🤝 المساهمة

المساهمات مرحب بها! يرجى فتح Issue أو Pull Request.

## 📄 الترخيص

هذا المشروع مرخص تحت [MIT License](LICENSE).

## 📧 التواصل

للاستفسارات والدعم، يرجى التواصل على [your-email@example.com]

---

تم البناء بـ ❤️ باستخدام React و Material-UI
