# بيانات الدخول - PSBrand

## 🔐 حساب الأدمن

للدخول إلى لوحة الأدمن:

```
اسم المستخدم: admin
كلمة المرور: admin
```

---

## 👥 حسابات الموظفين

**مهم:** الموظفون لا يملكون حسابات افتراضية!

### كيفية إنشاء حساب موظف:

1. سجل دخول كأدمن
2. اذهب إلى تبويب "إدارة الموظفين"
3. اضغط "إضافة موظف"
4. املأ البيانات:
   - الاسم الكامل
   - البريد الإلكتروني
   - رقم الهاتف
   - رقم الموظف
   - **اسم المستخدم** (مثال: ahmad123)
   - **كلمة المرور** (مثال: ahmad2024)
5. اضغط "إضافة"

### تسجيل دخول الموظف:

1. من الصفحة الرئيسية، اختر "موظف"
2. أدخل اسم المستخدم وكلمة المرور اللي حددها الأدمن
3. اضغط "تسجيل الدخول"

---

## 📝 ملاحظات مهمة

### للتطوير:
- هذه البيانات **للتطوير والاختبار فقط**
- يجب تغييرها في بيئة الإنتاج
- عند الربط مع الباك اند، سيتم استخدام نظام مصادقة آمن

### للإنتاج:
⚠️ **تحذير**: لا تستخدم هذه البيانات في بيئة الإنتاج!

يجب عليك:
1. تغيير username و password الافتراضية
2. استخدام كلمات مرور قوية (8+ أحرف، أرقام، رموز)
3. تفعيل التشفير (HTTPS)
4. إضافة Two-Factor Authentication (2FA)
5. استخدام JWT tokens آمنة
6. تسجيل جميع محاولات الدخول

---

## 🔧 كيفية تغيير البيانات الافتراضية

### في الكود الحالي:

افتح ملف `src/pages/AdminLogin.jsx` وعدّل السطر:

```javascript
// السطر 35-36
if (data.username === 'admin' && data.password === 'admin') {
```

غيّره إلى:

```javascript
if (data.username === 'YOUR_USERNAME' && data.password === 'YOUR_PASSWORD') {
```

### عند الربط مع الباك اند:

لن تحتاج للتعديل، سيتم التحقق من البيانات من خلال API:

```javascript
// مثال
const response = await authService.login({
  username: data.username,
  password: data.password,
  role: 'admin'
});
```

---

## 📊 مثال على بيانات موظف:

عند إضافة موظف، الأدمن يحدد:

```
الاسم: أحمد محمد
البريد: ahmad@psbrand.com
الهاتف: 0599123456
رقم الموظف: EMP001
اسم المستخدم: ahmad123    ← سيستخدمه للدخول
كلمة المرور: ahmad2024     ← سيستخدمها للدخول
```

بعدها الموظف يقدر يسجل دخول باستخدام:
- Username: `ahmad123`
- Password: `ahmad2024`

---

## 🛡️ نصائح الأمان

1. **لا تشارك بيانات الدخول** مع أحد
2. **غيّر كلمة المرور بانتظام**
3. **استخدم كلمات مرور قوية ومعقدة**
4. **لا تحفظ كلمات المرور في الكود**
5. **استخدم متغيرات بيئية** (Environment Variables)
6. **فعّل HTTPS في الإنتاج**

---

## 📧 الدعم

إذا نسيت كلمة المرور أو واجهت مشاكل في تسجيل الدخول، راجع ملف `USAGE.md`

